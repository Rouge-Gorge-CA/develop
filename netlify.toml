[build]
  command = "npm run build"
  publish = "dist"

# Décommenter et remplacer l'URL quand le backend Railway est déployé :
# [[redirects]]
#   from = "/api/*"
#   to = "https://TON-BACKEND.railway.app/api/:splat"
#   status = 200
#   force = true
